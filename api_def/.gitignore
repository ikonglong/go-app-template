# Smithy build artifacts — regenerated by `smithy build` / `make merge`.
# The `source` projection copies sources under build/smithy/source/sources/;
# keep it out of the tree so it is never committed or swept into a `cp -R`.
server_api/build/
